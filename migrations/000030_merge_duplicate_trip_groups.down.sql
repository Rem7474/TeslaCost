-- ============================================================================
-- TeslaCost Merge Duplicate Trip Groups Migration (Down)
-- ============================================================================

-- The merged duplicates cannot be told apart from the group they were merged into and are not restored.
SELECT 1;
