-- ============================================================================
-- TeslaCost Reset Toll Detections Migration (Down)
-- ============================================================================

-- The dropped detections are recomputed on demand from the GPS traces and are not restored.
SELECT 1;
